{
  "name": "ritual-agent-registry-contracts",
  "version": "0.1.0",
  "description": "Smart contracts for Ritual Agent Registry & Reputation Tracker",
  "main": "index.js",
  "scripts": {
    "build": "npx asb",
    "abi": "npx asb --abi",
    "test": "npx hardhat test",
    "lint": "eslint . --ext .ts",
    "format": "prettier --write \"**/*.ts\"",
    "deploy:devnet": "npx hardhat run scripts/deploy.js --network devnet",
    "deploy:mainnet": "npx hardhat run scripts/deploy.js --network mainnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.6.1",
    "asbuild": "^0.2.0",
    "assemblyscript": "^0.19.3",
    "dotenv": "^17.4.2",
    "hardhat": "^2.28.6",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "ethers": "^6.9.0"
  }
}
